import 'package:flame/components.dart';

class AppConfig {
  static const gravity = -6;
  static const double objSize = 10;
  static const double acceleration = -100;
  static final Vector2 shapeSize = Vector2.all(200);
}
